# TinyOlly Common Package Base Image
FROM python:3.12-slim as tinyolly-common

WORKDIR /tinyolly-common

# Copy the shared package
COPY setup.py .
COPY README.md .
COPY tinyolly_common/ tinyolly_common/

# Install the package in development mode
RUN pip install --no-cache-dir -e .
